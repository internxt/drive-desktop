{
  "name": "internxt-drive",
  "version": "2.5.0",
  "description": "Internxt client UI",
  "main": "./dist/main/main.js",
  "author": "Internxt <hello@internxt.com>",
  "scripts": {
    "link-modules": "ts-node ../../.erb/scripts/link-modules.ts",
    "rebuild": "electron-rebuild --parallel --force --types prod,dev,optional --module-dir .",
    "postinstall": "npm run rebuild && npm run link-modules"
  },
  "dependencies": {
    "better-sqlite3": "^8.3.0",
    "typeorm": "^0.3.16",
    "virtual-drive": "../../../node-win"
  },
  "license": "MIT",
  "devDependencies": {
    "@electron/rebuild": "^3.7.1"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3"
    ]
  }
}
